# Autogenerated by fastlane, loaded from the root Gemfile.
#
# No plugins: every lane in fastlane/lanes/ is built from core actions so the
# release path has one less moving dependency. Add plugins with
# `bundle exec fastlane add_plugin <name>` if that ever stops being true.
