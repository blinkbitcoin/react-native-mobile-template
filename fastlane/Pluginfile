# Autogenerated by fastlane, loaded from the root Gemfile.
#
# One plugin, and only because there is no alternative: Huawei ships no command
# line tool and fastlane has no core action for AppGallery Connect, so the
# `android upload_huawei` lane (fastlane/lanes/huawei.rb) is built on this
# community plugin. It is pinned to an exact version rather than a pessimistic
# constraint: a single maintainer and no documented stability promise mean a new
# version is something to read before it reaches a release. Every other lane in
# fastlane/lanes/ is built from core actions. Add plugins with
# `bundle exec fastlane add_plugin <name>`.
gem 'fastlane-plugin-huawei_appgallery_connect', '1.1.3'
